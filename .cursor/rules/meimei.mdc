---
description: meimei 项目全局规则——每次新对话自动加载，最高优先级
alwaysApply: true
---

# meimei 全局规则（每次对话自动生效）

优先级：用户当次明确指令 > 本文件 > 默认行为。
每次新对话开始任何工作前，先阅读仓库根目录的两份文件并相互对照：
- PLAN.md（决策清单：82 条决策 + 评审记录 + 提醒事项）
- BLUEPRINT.md（总设计图：节点施工图 + 22 项功能总清单）
两份文件内容冲突时，以编号更新的 PLAN.md 为准，并向用户确认后再动工。

## R1 工作流程（最高优先级）

- R1.1 计划先行：收到开发类需求，先输出计划（改动哪些文件、每步做什么），等用户明确说"执行/可以/开始"才动手；禁止未经批准直接写代码
- R1.2 若需要用户判断代码，代码块上方必须有一行中文说明"这段代码是干什么的"
- R1.3 信息不足时必须提问；禁止自行假设、禁止擅自添加计划外功能
- R1.4 禁止修改核心配置（config.py 既有键值、.env、数据库表结构），除非用户在计划中明确批准
- R1.5 节点 push：每完成一个主干节点/阶段，进入下一阶段前自动 commit + push 到 GitHub，并提醒用户
- R1.6 最终复审：产品最终完成前，以心理专家/产品经理/律师/普通用户四视角复审，用户确认后才算完成
- R1.7 PLAN.md 每次更新同步 push，一版一个 commit
- R1.8 自运行（元规则）：本文件每次新对话自动加载（alwaysApply: true）。任何对话开始任何工作前，必须通读本文件 + PLAN.md + BLUEPRINT.md；若发现本文件未被加载、规则之间冲突或规则与用户指令冲突，必须主动向用户声明，不得静默忽略
- R1.9 勘误机制：发现任何错误（方案/文档/代码/事实性错误，无论谁发现的）→ ①立即向用户指出错误内容与影响范围 → ②在 PLAN.md 末尾"勘误记录"追加一条（错误内容 → 修正内容 → 原因）→ ③修复并单独 commit（提交信息以"fix（勘误）:"开头）→ ④push 并告知用户。禁止悄悄改错不留痕

## R2 编码规范

- R2.1 技术栈以 requirements.txt 为准：Python 3.12 + FastAPI + zai-sdk + SQLite（标准库）；前端微信小程序（二期）
- R2.2 禁止引入 requirements.txt 之外的第三方库；确需引入，先在计划中声明并获用户批准
- R2.3 每个核心逻辑（模块/函数/非显然分支）必须有中文注释，说明"为什么这样做"，不复述代码
- R2.4 命名规范：文件/函数/变量用 snake_case，类用 PascalCase，常量用 UPPER_SNAKE_CASE；命名用英文，注释用中文
- R2.5 单个函数不超过 80 行，超出必须拆分
- R2.6 禁止重写整个文件；禁止重构与本次任务无关的模块；只做计划内的最小改动
- R2.7 代码风格与既有文件保持一致（4 空格缩进、导入顺序、引号风格）

## R3 架构约束（主干-分枝，防代码过乱）

- 主干五层，职责单一：config.py（配置）/ glm_client.py（AI 调用）/ prompts.py（prompt 资产）/ safety.py（安全边界）/ storage.py（数据）
- 新功能 = 新建独立文件 + main.py 注册一行，不改动其他模块
- 文案、语气、人设、情绪库 → 只改 prompts.py
- 边界与危机规则 → 只改 safety.py
- UI 相关事项 → 不进入后端代码，记入 PLAN.md"提醒事项"，二期集中讨论
