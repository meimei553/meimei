# meimei 总设计图（施工图）

> 这份文件是"试卷答案"级别的详细施工图：每个节点建什么文件、每个接口长什么样、
> 做完怎么验证、你的每条要求落在哪，全部写清。
> 配套文件：PLAN.md（80 条决策清单与评审记录）、.cursor/rules/meimei.mdc（全局规则）。

## 怎么读这张图

- **主干**：5 个基础文件，所有功能都建立在它们之上，建好后基本不动
- **分枝**：每个功能一个独立文件，互不干扰
- **节点**：施工的一步。每个节点做完 = 自动 push 到 GitHub + 提醒你验收
- **验证**：每个节点末尾写清"打开什么、点什么、应该看到什么"，你照着做就能检查

## 施工顺序总览

```
节点 0 主干骨架 → 节点 1 分析 → 节点 2 反思对话 → 节点 3 润色
→ 节点 4 记录与设置 → 节点 5 小程序骨架 → 节点 6 叶子（示例与合规文件）
```

---

## 节点 0：主干骨架

**做完后**：后端服务能启动，有健康检查接口；隐私文件（密钥、数据库）保证不进 git。

**要创建的文件**（每个文件干什么）：

1. `requirements.txt` —— 依赖清单，只有 5 个：fastapi、uvicorn、zai-sdk、python-dotenv、pydantic
2. `.gitignore` —— 告诉 git 永远不要上传：`.env`（密钥）、`*.db`（聊天记录数据库）、`__pycache__`
3. `.env.example` —— 配置模板，你复制一份改名 `.env` 填上自己的 Key
4. `backend/config.py` —— 配置层。读取 .env 里的 Key 和模型名；提供"是否演示模式"的判断
5. `backend/glm_client.py` —— AI 调用层。全项目只有它能跟 GLM 说话；负责把 AI 返回的文字解析成 JSON（带容错和 1 次重试）
6. `backend/prompts.py` —— Prompt 资产层（**全项目最重要的文件**）。包含：温柔人设、情绪库（8 个家族带强度梯度 + 复合情绪 + 社会情绪 + 中文高频词，约 50 个）、5 个语气预设、回应示例库（5~10 个精心编写的温柔回应，供 AI 模仿——模仿比描述稳定）、所有固定文案（等待语、减压语、"这里没有评判"、免责声明等）。以后想改 AI 的说话方式，只改这一个文件
7. `tests/tone_regression.md` —— 温柔回归测试集：20 个典型场景（崩溃/愤怒/质疑/深夜 emo……）的标准回应，**内容全部原创虚构**，不使用任何真实聊天记录；**每次改 prompts.py 后必须跑一遍**，发现回应变生硬立刻回滚；标准回应写完后**需用户验收"对味"**——人设质量是可测试的指标
8. `backend/safety.py` —— 安全边界层。四道防线：危机词识别（自伤/自杀关键词表 + AI 判断双保险）→ 输入校验（空、超 5000 字、乱码）→ 读不懂的内容 → 恶意内容
9. `backend/storage.py` —— 数据层。SQLite 建表和增删查（记录表：原文、背景、分析结果、对话历史、润色结果、改分轨迹、时间）
10. `backend/main.py` —— 入口。创建 FastAPI 应用、允许开发期跨域、健康检查 `/api/health`、注册各分枝路由

**怎么验证**：命令行运行 `uvicorn backend.main:app --reload` → 浏览器打开 `http://127.0.0.1:8000/docs` → 找到 `/api/health` 点"Try it out" → 看到 `{"status": "ok"}` 就是成功。

**落实的决策**：14、15、16、17、19、52（PLAN.md 决策清单编号，下同）

---

## 节点 1：分析分枝（核心功能）

**做完后**：粘贴任何内容（气话/好话/聊天记录）→ 得到完整分析结果 → 自动存进记录；危险内容被温柔拦截。

**要创建的文件**：`backend/analyze.py`（分析接口）；在 `prompts.py` 里新增分析 prompt

**接口**：`POST /api/analyze`

- 你发给它：`{"text": "粘贴的内容", "background": "背景（可选，不填也行）", "guess": "用户的情绪猜想（可选）"}`
- **情绪猜想对比（创新点，极轻量）**：用户粘贴后只需选"你觉得最主要的情绪是哪一个"
  + 强度滑块（轻微/明显/强烈/非常强烈，与结果页用词一致），一键完成；
  AI 分析返回后同屏对比——"你以为你很愤怒，原来你更多是委屈"；
  "跳过，直接分析"按钮显眼；文案："猜错才有意思：猜错的地方，就是你没发现的自己"
- 它返回给你（每个字段的含义）：
  - `emotions`：情绪列表，6~8 个，每个含：名称、占比（%）、强度（0-100）、强度人话（轻微/明显/强烈/非常强烈）、一句话解释
  - `my_analysis`：你为什么这样说（温柔解读）
  - `other_analysis`：对方可能在想什么（多种可能性 + "基于文字推测"标注；好话时克制）
  - `key_sentences`：点出最关键的 2~3 句原话
  - `comfort`：安慰语（第一眼先看到它——先情后理）
  - `guiding_questions`：2~3 个引导式反思问题
  - `review_advice`：复盘建议（"下次可以试试……"；好话时变成"延续建议"；严重伤害时转向"保护自己"）
  - `crisis`：是否命中危机信号。命中时上面都没有，只返回求助渠道和陪伴话术
  - `record_id`：这次分析自动存进记录的编号
  - `demo`：是否演示数据（没配 Key 时返回内置示例）

**分析 prompt 里写死的规矩**（都在 prompts.py）：开放推测不读心、四级人话、标签边界声明、无诊断声明、第三方隐私提示、先降温标记（情绪太激烈时引导提问和复盘建议标记为"延后"）、结果页固定提示"每次分析是那一刻的理解，同一内容多次分析分数可能略有波动"。

**怎么验证**：
1. /docs 里调用 `/api/analyze`，粘贴一段吵架记录 → 返回里 12 个字段齐全
2. 输入一句开心的话 → 对方推测以肯定为主、复盘建议变成延续建议
3. 输入含"不想活了"的内容 → 返回危机回应（热线 + 陪伴），不出分析
4. 输入"aaaa1111" → 温柔提示读不懂，不出分析
5. 不填 background → 正常分析；填了 → 解读里能看出结合了背景

**落实的决策**：1、2、3、4、5、6、7、26、27、29、32、33、34、39、40、64、68、74、76、77、78、79

---

## 节点 2：反思对话分枝

**做完后**：对分析结果有疑问可以追问，AI 引用原话解释；可以平等讨论改分（留痕）；一直聊，AI 不主动收尾。

**要创建的文件**：`backend/chat.py`；在 `prompts.py` 里新增对话 prompt

**接口**：`POST /api/chat`

- 你发给它：`{"record_id": 1, "message": "你为什么说我委屈？"}`
- 它返回：`reply`（AI 的回应）+ 可选的 `score_revision`（如果讨论后 AI 判断要改分，这里带新分数和理由，原分数保留不覆盖）

**对话 prompt 里写死的规矩**：引用原话解释依据；先接纳再说理；平等讨论不诡辩不迎合；改分要留痕；发现原地打转温柔建议休息；深度对话后合适时机提醒"真正的和解要回到现实关系里"；永不主动收尾。

**技术细节**：每次对话自动携带这条记录的原文 + 分析结果 + 聊天历史；超过约 20 轮时，自动把早期对话压缩成摘要（省钱，你无感知）；每句对话都存进记录。

**怎么验证**：
1. 先分析得到 record_id → 追问"为什么说我委屈" → AI 引用你的原话解释
2. 说"我不是委屈，我是生气" → AI 和你讨论，可能改分并留痕（记录里能看到原始分和修改轨迹）
3. 连续聊 5 轮以上 → AI 不主动结束话题

**落实的决策**：9、25、28、30、31、35、36、37、65

---

## 节点 3：润色分枝

**做完后**：任何一句话都能润色成温柔基调的高情商版本；可指定语气；不满意可以接着提意见。

**要创建的文件**：`backend/polish.py`；在 `prompts.py` 里新增润色 prompt

**接口**：`POST /api/polish`

- 你发给它：`{"text": "要润色的话", "background": "可选", "tone": "可选", "feedback": "可选", "record_id": "可选"}`
- 它返回：`versions`（2~3 个版本，每个标注用了什么语气）+ `auto_tone`（语气是否 AI 自动判断）+ `record_id`

**规矩**：底层用非暴力沟通框架（观察→感受→需要→请求）；不指定语气时 AI 结合分析自动组合 2~3 种；指定语气支持预设（温柔但坚定/幽默化解/简洁直接/正式得体/撒个娇）或自然语言描述；带 feedback 时按意见重改并有明显变化；传 record_id 就合并进那条记录，否则新建。

**怎么验证**：
1. 直接润色"你到底还回不回家吃饭？" → 返回 3 个温柔系版本
2. 带 `tone: "humorous"` → 只返回幽默化解版本
3. 带 `feedback: "太正式了，像写公文"` → 新版本明显变口语

**落实的决策**：8、10、28、75

---

## 节点 4：记录与设置分枝

**做完后**：所有分析/对话/润色都能回看、筛选、删除；可以设置称呼；可以提反馈。

**要创建的文件**：`backend/records.py`、`backend/settings.py`

**接口**：
- `GET /api/records?emotion=委屈` —— 记录列表，可按情绪筛选，最新在前
- `GET /api/records/{id}` —— 一条记录的完整内容（含分析、对话历史、润色、改分轨迹）
- `DELETE /api/records/{id}` —— 删除，想删就删得掉
- `GET/PUT /api/settings` —— 读取/修改：你的称呼（AI 不使用名字，自称一律为"我"）
- `POST /api/feedback` —— 提交反馈，存进数据库

**怎么验证**：分析几次 → 列表里看到全部 → 按"委屈"筛选只剩相关记录 → 删除一条后再查返回 404 → 修改称呼后再分析，AI 用新称呼回应。

**落实的决策**：11、12、13、14、38、43、44、63、66

---

## 节点 5：小程序骨架

**做完后**：微信开发者工具里能跑通完整流程（只做功能，不做美化，UI 二期一起讨论）。

**要创建的文件**：`miniprogram/` 目录下 5 个页面 + 全局配置

1. **首次打开**：免责声明 + 隐私政策弹窗，点"同意"才进入（留痕）
2. **首页**：内容框（"把聊天记录或你想说的话放这里"）+ 背景框（可选，带示例）+ "开始分析"大按钮 + "直接润色"小入口 + "试试示例" + 开场白模板 + "完全免费" + "这里没有评判，只有理解"；等待时显示渐进文案（"我正在认真读你的话……"）
3. **分析结果页**：第一眼是安慰语，然后雷达图（先用文字版展示分数），再解读；引导提问可以直接点，点了就进对话
4. **记录列表页**：情绪筛选、删除、空状态引导、"这是你第 N 次认真面对自己的情绪"
5. **记录详情页**：完整分析 + 对话历史 + 继续对话；看负面记录时开头有温柔承接
6. **设置页**：称呼设置 + 反馈入口

**全局配置**：`app.js` 里集中写后端地址 `apiBase`（开发期 `http://127.0.0.1:8000`）；`project.config.json` 里填你的 AppID。

**怎么验证**：微信开发者工具导入 `miniprogram/` → 勾选"不校验合法域名" → 首页粘贴内容 → 分析 → 点引导问题进对话 → 润色 → 记录里看到这一条 → 删除。

**落实的决策**：41、42、45、55、59、67、69、70、72 及页面结构全部

---

## 节点 6：叶子（示例与合规文件）

**做完后**：产品完整，合规文件齐全。

1. 示例数据完善（"试试示例"跑通全流程）
2. `PRIVACY.md` 简版隐私政策：数据只存设备、不出设备、可删除、不联网统计
3. 免责声明与用户协议文案（首次弹窗用；含"AI 生成仅供参考，不构成心理/医疗/法律建议"；
   危机识别为关键词 + AI 双保险但无法承诺零漏判，与求助引导一起呈现；AI 生成内容著作权归用户）
4. `LICENSE`：MIT 开源协议
5. 结果页"本内容由 AI 生成"标识
6. README 最终完善

**落实的决策**：47、48、51、52、53、62

---

## 功能总清单（你的所有要求，逐项核对用）

施工完成后，这张表每一项都应该能打勾：

- [ ] 1. 粘贴聊天记录/气话/好话都能分析，AI 自动分辨谁说的
- [ ] 2. 可选背景框（示例引导、不填也没关系）
- [ ] 3. 动态情绪雷达图：AI 从情绪库选占比最高的 6~8 个，占比 + 强度 + 四级人话
- [ ] 4. "我为什么这样说"温柔解读
- [ ] 5. "对方可能在想什么"（多种可能性 + 免责标注；好话时克制）
- [ ] 6. 点出最关键的 2~3 句原话
- [ ] 7. 安慰语（第一眼看到）
- [ ] 8. 引导式反思提问（点击直接进对话）
- [ ] 9. 复盘建议（面向未来；好话变延续建议；严重伤害转向保护自己）
- [ ] 10. 反思对话：引用原话解释、平等讨论改分留痕、永不主动收尾、反刍提醒、依赖提醒、20 轮压缩
- [ ] 11. 润色：独立入口、NVC 框架、AI 自动组合语气、预设/自然语言语气、多轮微调、2~3 个版本
- [ ] 12. 记录：自动保存、情绪筛选、删除、回看温柔承接、陪伴文案、完成感确认
- [ ] 13. 称呼系统（你的称呼 + AI 名字可改）
- [ ] 14. 反馈入口
- [ ] 15. "试试示例"一键体验
- [ ] 16. 危机干预：识别 → 求助渠道 → 持续陪伴
- [ ] 17. 边界处理：空/超长/乱码/读不懂/恶意内容，全程温柔
- [ ] 18. "完全免费"标识、"这里没有评判"承诺、"AI 生成"标识、免责声明弹窗
- [ ] 19. 隐私政策、用户协议、MIT LICENSE
- [ ] 20. 等待渐进文案、空状态引导
- [ ] 21. 演示模式（没配 Key 也能跑通）
- [ ] 22. 小程序 5 页骨架打通全流程
- [ ] 23. 情绪猜想对比：先猜后揭示，"你以为的 vs 实际上的"同屏对照
- [ ] 24. 温柔人设防崩塌：回应示例库 + 20 场景回归测试集

## 施工纪律（每个节点都必须遵守）

1. 每完成一个节点 → 自动 commit + push 到 GitHub → 提醒你验收
2. 改文案/语气/情绪词 → 只动 `prompts.py`；改边界规则 → 只动 `safety.py`
3. 新功能 = 新文件 + main.py 注册一行，不动其他模块
4. 计划外不加功能；信息不足先问你
5. 声明与实现严格一致：实现变了，声明文案先改（防虚假宣传）
6. 节点 1 过大时，"猜想对比"允许降级到节点 6 之后补——主干优先，分枝可延后
7. 全部规则详见 `.cursor/rules/meimei.mdc`（每次对话自动生效）
